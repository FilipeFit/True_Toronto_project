<%= bootstrap_form_for @post, :html => {:multipart => true} do |f| %>

  <%= f.text_field :title, label: "Titulo"%>

  <%= f.text_area :content, label: "Conteudo"%>

  <%= f.file_field :image, label: "Imagem"%>

  <%= f.submit "Salvar", class: "btn btn-primary btn-xs"%>
<% end %>
